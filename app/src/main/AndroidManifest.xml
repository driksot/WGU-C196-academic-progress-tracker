<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.academicprogresstracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AcademicProgressTracker">
        <activity android:name=".UI.CourseAddEditActivity"></activity>
        <activity android:name=".UI.TermAddEditActivity" />
        <activity android:name=".UI.AssessmentDetailActivity" />
        <activity android:name=".UI.CourseDetailActivity" />
        <activity android:name=".UI.TermDetailActivity" />
        <activity android:name=".UI.AssessmentListActivity" />
        <activity android:name=".UI.CourseListActivity" />
        <activity android:name=".UI.TermListActivity" />
        <activity android:name=".UI.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>